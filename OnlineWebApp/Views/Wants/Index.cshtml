@model IEnumerable<OnlineWebApp.Models.AppModels.Want>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DriverInfos.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Order.Username)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Solo.StaffName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HasDriver)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HasStaff)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DriverInfos.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Order.Username)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Solo.StaffName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HasDriver)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HasStaff)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.WantId }) |
            @Html.ActionLink("Details", "Details", new { id=item.WantId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.WantId })
        </td>
    </tr>
}

</table>
