@model IEnumerable<OnlineWebApp.Models.AppModels.Point>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Items.Item_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cost)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.point)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.payementstatues)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Items.Item_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cost)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.point)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.payementstatues)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PointID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PointID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PointID })
        </td>
    </tr>
}

</table>
