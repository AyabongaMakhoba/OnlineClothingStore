@model IEnumerable<OnlineWebApp.Models.Order>


@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Username)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LastName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Phone)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OrderDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CollectionDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Collected)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Packed)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Option)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.Username)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.FirstName)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.LastName)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Phone)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.OrderDate)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.CollectionDate)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Collected)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Packed)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Email)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Option)
    </td>
    <td>
        <p style="font-size:large; text-align:right">
            @if (User.IsInRole("Admin"))
            {
                @*if (item.Packed == true)
                    {
                        <a href="@Url.Action(actionName: "Pack", controllerName: "Orders", routeValues: new { id = item.Order_Id })" class="btn disabled btn-default">Packed</a>
                    }
                    else
                    {
                        <a href="@Url.Action(actionName: "Pack", controllerName: "Orders", routeValues: new { id = item.Order_Id })" class="btn btn-default">Not Packed</a>
                    }*@
            } 
            <a href="@Url.Action(actionName: "Details", controllerName: "Orders", routeValues: new {id = item.Order_Id})" class=" btn btn-default" style="background-color:whitesmoke">Details</a> 
            @*@if (User.IsInRole("Admin"))
                {
                    if (item.Packed == false)
                    {
                    <a href="@Url.Action(actionName: "Collect", controllerName: "Orders", routeValues: new { id = item.Order_Id })" class=" btn disabled btn-default" style="background-color:darkgray">Collect</a>
                    }
                    else
                    {
                    <a href="@Url.Action(actionName: "Collect", controllerName: "Orders", routeValues: new { id = item.Order_Id })" class=" btn btn-default" style="background-color:darkgray">Collect</a>
                    }
                }*@
        </p>
    </td>
</tr>
    }

</table>
