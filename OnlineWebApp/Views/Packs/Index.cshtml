@model IEnumerable<OnlineWebApp.Models.AppModels.Pack>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Order_Id)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Order_Id)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PackingID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PackingID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PackingID })
        </td>
    </tr>
}

</table>
